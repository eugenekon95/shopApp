
<div class="row">
  <%= render 'categories/category'  %>
  <div class="col-9">
    
    <% @categories.each do |categ|  %>
      <% next if categ.products.count <= 0 %>
      <h4 class="mt-4"><%= categ.name %></h4>
      <div class="row">
        <%categ.products.each do |product| %>
            <%= render product,  category: categ %>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
